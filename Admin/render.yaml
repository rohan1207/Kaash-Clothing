services:
  - type: web
    name: kaash-admin
    env: static
    buildCommand: NODE_VERSION=18 npm install && npm run build
    staticPublishPath: ./dist
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
